<h1> All Guests </h1>
<hr>
<h3> <%= link_to "Click here for Episodes", "/episodes/" %></h3>
<hr>
<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to guest.name, guest_path(guest) %></li><br>
  <% end %>
</ul>
