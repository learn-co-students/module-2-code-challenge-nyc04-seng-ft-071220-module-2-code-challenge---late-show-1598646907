<h1> Episode Page </h1>

# A list of the guests who were on that episode
# Each guest's name should link to the Guest Show page

<%= @episode.date %>

# Iterate through all guests of the specific episode and link it to the Guest Show Page.

<ul>
<% @episode.guests.each do |guest| %>
    <li>
    <%= link_to guest.name, guest_path(guest) %>
    </li>
<% end %>
</ul>


#7 On the Episode show page, show the average rating of the Appearances for the episode.

# Iterate through appearances of the specific episode and use .average(:rating) to find an average rating